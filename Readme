Файл example.txt должен имееть следующий вид:

<time> <command>
<time> <command>
<time> <command>
...

<time> - обозначается одним числом. Означает время, через которое программа будет выполнять команду, написанную в данной строчке после <time>;
<command> - команда для терминала;
Файл ничем не заканчивается, представляет из себя просто набор строк.
Пример:

2 ls
3 echo hellow
1 echo world
